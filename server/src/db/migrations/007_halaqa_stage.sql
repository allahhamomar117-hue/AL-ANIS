-- =====================================================================
-- 007 | المرحلة الدراسية للحلقة
--
-- تحلّ محلّ الموعد والمكان في نموذج الحلقة: primary | preparatory | secondary
-- (ابتدائي | إعدادي | ثانوي). العمود نصّي بلا CHECK لأن ALTER TABLE في
-- SQLite لا يعيد بناء الجدول، والتحقق مطبَّق في طبقة الـ API (zod).
-- عمودا schedule_time و location يبقيان لحفظ البيانات القديمة.
-- =====================================================================

ALTER TABLE halaqat ADD COLUMN stage TEXT;
