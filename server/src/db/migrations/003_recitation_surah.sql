-- =====================================================================
-- 003 | التسميع بالسور (جزء عمّ)
--
-- يُضاف عمود واحد فقط: رقم السورة (78..114) أو NULL للتسميع بالصفحة.
-- التسميع بالسورة يُخزَّن أيضاً بصفحاته (page_number / to_page) المشتقّة
-- من موضع السورة في المصحف، فتبقى التقارير والفهارس والاستعلامات القائمة
-- على الصفحات عاملة كما هي دون أي تغيير، ويبقى عمود type ضمن قيمه
-- المسموحة فلا حاجة لإعادة بناء الجدول (CHECK لا يُعدَّل في SQLite).
-- =====================================================================

ALTER TABLE recitations ADD COLUMN surah_number INTEGER;

CREATE INDEX IF NOT EXISTS idx_recitations_surah ON recitations (surah_number);
