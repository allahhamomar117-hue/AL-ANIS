# ── الأنيس | توجيه Netlify ───────────────────────────────────────────
# نسخة مطابقة لقواعد netlify.toml (الأخير له الأسبقية عند الاختلاف).
# الترتيب مهم: أول قاعدة تطابق هي التي تُنفَّذ.

# 1) الـ API → دالة Netlify التي تلفّ خادم Express.
/api/*  /.netlify/functions/api/:splat  200!

# 2) بقية المسارات → index.html ليتولّاها React Router.
/*      /index.html                     200
