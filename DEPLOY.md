# نشر الأنيس بنسختين

المشروع يُنشر مرّتين من نفس الشيفرة، والفارق كلّه في متغيّرات البيئة:

| | نسخة المسجد (Production) | نسخة العرض (Demo) |
|---|---|---|
| الغرض | الاستعمال الحقيقي | المعرض والـ HR |
| ملف القاعدة | `DB_FILE=data/anis.db` | `DB_FILE=data/demo.db` |
| البيانات | حقيقية، لا تُمسّ | `npm run db:seed-demo` |
| `JWT_SECRET` | مفتاح خاص | مفتاح **مختلف** |

ملفّا القاعدة منفصلان، فتشغيل بذرة العرض لا يمسّ بيانات المسجد إطلاقاً.

---

## محلياً

```bash
npm install && npm run api:install
cp .env.example .env
cp server/.env.example server/.env

# نسخة العرض: بيانات جاهزة ممتلئة
DB_FILE=data/demo.db npm run db:seed-demo
DB_FILE=data/demo.db npm run api      # نافذة
npm run dev                            # نافذة أخرى
```

حسابات نسخة العرض — كلمة المرور للجميع `123456`:

| الدور | اسم الدخول | ما يراه |
|---|---|---|
| مدير | `demo_admin` | كل شيء + إدارة حسابات الكادر |
| مشرف | `demo_supervisor` | كل الحلقات والتقارير، بلا إدارة حسابات |
| أستاذ | `demo_teacher` | حلقتاه فقط (الفرقان والنور) |

البذرة تولّد ٣ حلقات و١٥ طالباً و٦٠ يوماً من الحضور والتسميع والنقاط
بتواريخ ماضية حقيقية، فتظهر لوحة الصدارة والتقارير ممتلئة. التوليد حتميّ:
كل تشغيل يعطي نفس الأرقام، فلقطات المعرض تبقى مطابقة.

---

## على Render أو Railway

كل نسخة = خدمتان: خادم (Web Service) وواجهة (Static Site).

### ١. الخادم

| | |
|---|---|
| Root Directory | `server` |
| Build | `npm ci && npm run build` |
| Start | `npm run start` |

المتغيّرات (انظر `server/.env.example` للتفاصيل):

```
NODE_ENV=production
DB_FILE=data/demo.db                 # أو data/anis.db للمسجد
UPLOADS_DIR=data/uploads
JWT_SECRET=<ولّد مفتاحاً — ومختلفاً لكل نسخة>
CORS_ORIGINS=https://<نطاق-الواجهة>
```

`PORT` تحقنه المنصّة تلقائياً، فلا تضبطه.

> **القرص:** القاعدة SQLite ملفّ على القرص، وأقراص المنصّات مؤقتة —
> يُمسح المحتوى مع كل إعادة نشر. اربط **Persistent Disk** (Render) أو
> **Volume** (Railway) بمجلّد `server/data`. لنسخة العرض هذا مقبول حتى
> بلا قرص: يكفي إعادة تشغيل البذرة. لنسخة المسجد القرص إلزاميّ.

بعد أول نشر لنسخة العرض، شغّل مرّة واحدة من طرفية المنصّة:

```bash
npm run db:seed-demo
```

### ٢. الواجهة

| | |
|---|---|
| Root Directory | جذر المستودع |
| Build | `npm ci && npm run build` |
| Publish | `dist` |

```
VITE_API_BASE_URL=https://<نطاق-الخادم>
```

بلا شرطة مائلة في النهاية وبلا `/api` — العميل يضيفه. متغيّرات `VITE_`
تُدمج في الملفات وقت البناء وتصبح مقروءة للجميع، فلا تضع فيها أسراراً،
وأعد البناء بعد أي تغيير فيها.

---

## تحقّق بعد النشر

1. `https://<نطاق-الخادم>/api/health` يعيد `{"status":"ok"}`.
2. الدخول بحساب `demo_admin` / `123456` على نسخة العرض.
3. لوحة التحكم تُظهر أرقام اليوم، والتقارير تُظهر صدارة غير فارغة.

خطأ CORS في الطرفية معناه أن `CORS_ORIGINS` على الخادم لا يطابق نطاق
الواجهة تماماً (البروتوكول والنطاق، بلا شرطة في النهاية).
